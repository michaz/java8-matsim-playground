addons:
  apt:
    packages:
      - graphviz
  artifacts: true
  s3_region: "Frankfurt"
language: java
jdk:
  - oraclejdk8
script:
  - curl https://raw.githubusercontent.com/Factual/drake/master/bin/drake > drake
  - chmod 755 drake
  - ./drake -a guice.svg
